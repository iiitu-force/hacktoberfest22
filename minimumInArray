#include<bits/stdc++.h>
using namespace Std;
int main()
{
  int n;
  cin>>n;
  vector<int>v(n);
  for(int i=0;i<n;i++)
  {
      cin>>v[i];
  }
  
  int mini = INT_MAX;
  for(int i=0;i<n;i++)
  {
     if(v[i]<mini) mini = v[i];
  }
  cout<<mini<<"\n";
return 0;
}
